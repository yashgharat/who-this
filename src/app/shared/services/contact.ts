export interface Contact {
    user: string;
    contact_name: string;
    contact_number: string;
    contact_email: string;
}

export interface getContact {
    id: string;
    name: string;
    number: string;
    email: string;
}
