export interface sendContact {
    contact_name: string;
    contact_number: string;
    contact_email: string;
}

export interface Contact {
    id: string;
    name: string;
    number: string;
    email: string;
}
